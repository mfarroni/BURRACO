node_modules/
dist/
.env
.env.*
!.env.example
*.log
# NB: drizzle/meta/ (journal + snapshot) DEVE essere versionato: senza il journal
# `npm run db:migrate` fallisce con "Can't find meta/_journal.json".
